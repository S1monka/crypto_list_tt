import 'package:flutter/material.dart';

abstract class AppColors {
  static const green = Color(0xFF2DBD85);
  static const red = Color(0xFFE44358);

  static const white = Colors.white;
  static const grey = Color.fromRGBO(255, 255, 255, 0.4);
}
